# Runtime dependencies for django-lokdown (see pyproject.toml [project.dependencies])
# Consumers should pin exact versions in production, e.g. django-lokdown==1.1.0 and
# pip-compile/pylock a transitive lockfile; run `pip-audit` regularly on that lockfile.
Django>=5.1
django-ratelimit>=4.1.0
djangorestframework>=3.16.1
djangorestframework-simplejwt>=5.5.1
drf-spectacular>=0.28.0
pyotp>=2.9.0
qrcode>=8.2
webauthn>=2.6.0
Pillow>=11.3.0
cryptography>=43.0.0
django-allauth[headless]>=65.0.0

# Example app and development tools (see pyproject.toml [project.optional-dependencies.dev])
django-cors-headers>=4.7.0
black==26.5.1
flake8>=7.3.0
pytest>=8.4.0
pytest-django>=4.11.0
pip-audit>=2.9.0
