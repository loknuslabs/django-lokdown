# Runtime dependencies for django-lokdown (see pyproject.toml [project.dependencies])
# Consumers should pin exact versions in production, e.g. django-lokdown==1.3.0 and
# pip-compile/pylock a transitive lockfile; run `pip-audit` regularly on that lockfile.
Django>=6.0.6
django-ratelimit>=4.1.0
djangorestframework>=3.17.1
djangorestframework-simplejwt>=5.5.1
drf-spectacular>=0.29.0
pyotp>=2.9.0
qrcode>=8.2
webauthn>=2.7.1
Pillow>=12.2.0
cryptography>=48.0.0
django-allauth[headless]>=65.18.0

# Example app and development tools (see pyproject.toml [project.optional-dependencies.dev])
django-cors-headers>=4.9.0
black==26.5.1
flake8>=7.3.0
mypy>=1.19.0
django-stubs>=5.2.0
djangorestframework-stubs>=3.16.0
pytest>=9.0.3
pytest-django>=4.12.0
pip-audit>=2.10.0
